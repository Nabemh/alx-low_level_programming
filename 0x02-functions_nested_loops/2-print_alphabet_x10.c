#include "main.h"

/**
 * void: Print 10 times the alphabet, in lowercase, followed by a new line.
 */

void print_alphabet_x10(void)
{
	char alphabet;
	int i = 0;

	for(i < 10; alphabet = 'a'; alphabet <= 'z'; alphabet++);
		_putchar(alphabet);
	_putchar("\n")
}
